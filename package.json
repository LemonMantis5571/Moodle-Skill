{
  "name": "moodle-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "University-agnostic Moodle CLI tool adapter",
  "bin": {
    "moodle-tools": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  }
}
